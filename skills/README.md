# Shared skills

Place standalone, reusable skills here as `skills/<name>/SKILL.md` (plus any
supporting files). These aren't tied to a single plugin and can be pulled in
by multiple plugins/agents in this repo over time.

Empty for now — add the first skill folder when needed.
